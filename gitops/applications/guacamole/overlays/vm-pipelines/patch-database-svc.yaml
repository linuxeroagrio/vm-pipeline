apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: databse
spec:
  volumeClaimTemplates:
  - metadata:
      name: pg-data
    spec:
      accessModes:
      - "ReadWriteOnce"
      resources:
        requests:
          storage: 1Gi
      storageClassName: nfs