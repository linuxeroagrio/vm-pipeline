apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "virtual-desktop.fullname" . }}-test-connection"
  labels:
    {{- include "virtual-desktop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "virtual-desktop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
